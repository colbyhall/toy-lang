struct Foo {
    a: i32,
    b: char,
	c: f32,
	d: String,
}

// Single Line Comment
/** 
 * Multi Line Comment 
 * /* nested */
 */

fn main() {
    let mut foo = Foo{
        a: 42,
        b: '😀',
		c: 450.0,
		d: "Hello World",
    };

	fn bar(foo: Foo) -> bool {
		return foo > 43;
	}

    // if bar(foo) {
	// 	foo.a = 123;
	// } else {
	// 	foo.c = 45.0;
	// }
}