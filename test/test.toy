struct Foo {
    a: i32,
    b: char,
	c: f32,
	d: String,
	e: bool,
	f: Bar,
}

struct Bar {
	a: u64,
}

// Single Line Comment
/** 
 * Multi Line Comment 
 * /* nested */
 */

fn main() {
	let mut foo = Foo{
		a: 42,
		b: '😀',
		c: 450.0,
		d: "Hello World",
		e: false,
		f: Bar {
			a: 123456789
		}
	};

	struct Car {
		name: String,
		speed: f32,
		is_fast: bool,
	}

	fn test(car: Car) -> bool {
		true;
	}

	let foo = if foo.e {
		let car = Car {
			name: "Yo Mama",
			speed: 320.0,
			is_fast: true,
		};

		test(car);
	} else if false {
		println("Hello World");
	} else {
		panic();
	};
}
